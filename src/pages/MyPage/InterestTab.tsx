import React from 'react';
import "../../css/MyPage.scss";


const InterestTab = () => {
    // const { interestedArticles } = useInterest();

     return (
        <div>
            {/*<ul>*/}
            {/*    {interestedArticles.map(article => (*/}
            {/*        <li key={article.id}>*/}
            {/*            <h3>{article.title}</h3>*/}
            {/*            <img src={process.env.PUBLIC_URL + `/assets/thumbnail/${article.thumbnail}`} alt={article.title} />*/}
            {/*        </li>*/}
            {/*    ))}*/}
            {/*</ul>*/}
        </div>
    );
};

export default InterestTab;
